[project]
name = "youth-profile-system"
version = "0.1.0"
description = "Youth profiling system with Django and FastAPI deployment entrypoints."
requires-python = "~=3.12.0"

[project.scripts]
app = "app:app"
